#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    // 13597 Tri-du
    // 수학
    int a, b;
    cin >> a >> b;
    cout << max(a, b);

    return 0;
}
