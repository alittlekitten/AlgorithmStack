#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 33169 所持金 (Money On Me)
	// 수학
	int a, b;
	cin >> a >> b;
	cout << a * 1000 + b * 10000;

	return 0;
}
