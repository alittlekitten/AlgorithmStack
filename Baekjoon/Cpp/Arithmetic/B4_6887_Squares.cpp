#include <iostream>
#include <cmath>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 6887 Squares;
	// 수학
	int n;
	cin >> n;
	cout << "The largest square has side length " << (int)sqrt(n) << ".";

	return 0;
}
