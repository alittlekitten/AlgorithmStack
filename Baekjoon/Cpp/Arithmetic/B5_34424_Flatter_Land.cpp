#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 34424 Flatter Land
	// 수학
	int n, x;
	cin >> n >> x;
	cout << (n - 1)* x;

	return 0;
}
