#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 34380 Blaster's Dash
	// 수학
	int m, v, n, k;
	cin >> m >> v >> n >> k;
	cout << 40 + n * 2;

	return 0;
}
