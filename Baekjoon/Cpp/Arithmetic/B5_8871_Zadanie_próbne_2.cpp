#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	// 8871 Zadanie próbne 2
	// 수학
	int n;
	cin >> n;
	cout << 2 * (n + 1) << " " << 3 * (n + 1);

	return 0;
}
