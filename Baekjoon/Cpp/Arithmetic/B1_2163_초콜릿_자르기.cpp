#include <iostream>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(NULL);

    // 2163 초콜릿 자르기
    // 수학
    int n, m;
    cin >> n >> m;
    cout << n * m - 1;

    return 0;
}
