#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(NULL);

    // 15700 타일 채우기 4
    // 수학
    long long n, m;
    cin >> n >> m;
    cout << n * m / 2;

    return 0;
}
