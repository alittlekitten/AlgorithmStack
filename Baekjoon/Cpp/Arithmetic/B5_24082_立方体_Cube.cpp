#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	// 24082 立方体 (Cube) 
	// 수학
	int x;
	cin >> x;
	cout << x * x * x;

	return 0;
}
