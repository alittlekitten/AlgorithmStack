#include <iostream>
#include <string>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 2743 단어 길이 재기
	// 문자열

	string a;
	cin >> a;
	cout << a.size();

	return 0;
}
