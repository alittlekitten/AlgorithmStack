#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 30010 잘못된 버블정렬
	// 애드혹
	int n;
	cin >> n;

	for (int i = n; i > 0; --i) cout << i << " ";

	return 0;
}
