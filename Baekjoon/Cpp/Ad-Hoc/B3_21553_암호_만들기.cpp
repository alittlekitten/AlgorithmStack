#include <iostream>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	// 21553 암호 만들기
	// 애드혹
	string a, p;
	cin >> a >> p;

	string b = p;
	cout << b;

	return 0;
}
