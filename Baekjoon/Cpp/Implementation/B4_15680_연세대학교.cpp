#include <iostream>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(NULL);

    // 15680 연세대학교
    // 구현

    int a;
    cin >> a;
    if (a == 0) cout << "YONSEI";
    else if (a == 1) cout << "Leading the Way to the Future";

    return 0;
}
